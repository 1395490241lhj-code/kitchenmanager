# PROJECT_STATUS.md

Last updated: 2026-07-14 (Phase 2B-8)

This file tracks the current project state for humans and AI coding agents. Update it after meaningful progress.

---

## 1. Current Stage

Kitchen Manager is no longer an empty early prototype. It is a functional local-first PWA with an existing feature set, tests, deployment workflow, and project guide documents.

Current focus:

- Make future AI-assisted development safer and more consistent.
- Preserve the working app while iterating.
- Keep project context, coding rules, testing rules, and handoff status inside the repository.
- Avoid regressions in the core cooking/inventory/shopping loop.
- Continue the native SwiftUI app under `ios-native/` without changing the PWA's stable routes or storage contracts.
- The native inventory now includes a persistent pantry-staple workflow: thresholds, restock quantities, low/out-of-stock states, suggestions, shopping-list merging, transition notifications, and backup/restore all use the existing `KitchenStore` and inventory records.
- The native core experience is aligned more closely with the PWA: actionable recipe/shopping add controls, Home quick recording, real ingredient/seasoning separation, seasoning-aware shopping generation, dual-mode staples, curated/full libraries, native recipe filters, inventory matching, and user override priority.
- Native recipe classification now treats `seasonings` as “调料与辅料”, with conservative title/context-aware handling for ambiguous ingredients. Home quick actions share one bottom-sheet presentation model, and Settings exposes only consumer-facing controls outside debug builds.
- Native inventory entry now shares one compact ingredient parser and one conservative expiry-suggestion path: compact quantity/unit input, receipt confirmation, manual batch entry, shopping stock-in, and normal inventory additions preserve explicit dates while suggesting dates only for recognized fresh foods.
- `InventoryExpirySuggestion` now covers every ordinary ingredient category (fresh produce, meat/seafood, dairy/egg/tofu, frozen, bread, deli, cured meat, opened/unopened seasonings, pantry staples) with a finite day count; only `常备` items and truly empty names still return `nil`, and unrecognized names default to 7 days. The manual-entry and receipt-confirmation "设置保质期" toggle is gone — both flows always show a plain `保质期` `DatePicker`, and a `hasUserEditedExpiry` flag (set only by real `DatePicker` interaction) protects a user's manual date choice from being overwritten by further name edits.
- The receipt confirmation list now renders each recognized item as a compact two-line row (`ReceiptIngredientCompactRow`) inside one shared `Section`, replacing the old one-`Section`-per-item layout that made each row ~200pt+ tall; rows are now ~78-96pt, fitting several more items per screen while keeping the same top-level 全选/确认入库 chrome and per-item delete.
- Native Inventory now presents fresh food as urgency-sorted adaptive lifecycle cards with dynamic light/dark status surfaces, a compact expiry progress line, accessible combined labels, and confirmed deletion. Staple rows retain their separate threshold-progress semantics.
- Native inventory detail now has one value-based single-item navigation destination and a user-controlled expiry-date editor; Home expiry and pending-shopping status sheets share the same material/grouped-list presentation container.
- Native iOS inventory persistence has completed its first SwiftData phase. `InventoryItem` remains the Codable/Hashable business and backup model, while an injected persistence layer stores every current inventory field in `InventoryRecord`. Startup migrates `native_km_inventory_v1` once, verifies UUIDs/counts before marking completion, preserves legacy JSON, and merges missing UUIDs without collapsing same-name batches. Other native data modules remain on their existing stores.
- Native iOS shopping-list persistence has completed its second SwiftData phase. `KitchenShoppingItem` remains the business and backup model, `ShoppingItemRecord` stores its current fields plus persistence-only order metadata, and startup migrates `native_km_shopping_v1` idempotently without deleting the legacy JSON. Shopping mutations, recipe/week-plan batch generation, completed-item stock-in, backup restore, and local-data clearing now use the injected shopping persistence layer; plans, weekly menus, consumption records, recipes, and settings remain on their prior stores.
- Native iOS today-plan persistence has completed its third SwiftData phase. `MealPlanItem` and the version-1 backup payload remain unchanged, while `TodayPlanRecord` mirrors its six business fields plus persistence-only order metadata. Startup migrates `native_km_plans_v1` behind `native_km_today_plan_swiftdata_migration_v1`, keeps legacy JSON, and preserves UUID-based duplicate semantics and array order. Home, recipe detail, AI cooking, week-plan batch insertion, shopping generation, consumption completion, backup restore, and local-data clearing continue through `KitchenStore`. Weekly plans, consumption records, user recipes, favorites, and settings remain on their existing stores.
- Native iOS consumption-record persistence has completed its fourth SwiftData phase. `InventoryConsumptionRecord` and its item array remain the Codable business/backup models; `ConsumptionRecordEntity` stores the record fields plus encoded `planIDs`/items and persistence-only order metadata. Startup migrates `native_km_consumption_records_v1` idempotently behind `native_km_consumption_swiftdata_migration_v1`, retaining the legacy JSON. Cooking confirmation and undo now calculate local snapshots, persist inventory then consumption records, and publish only after both succeed; a failed second write rolls inventory back best-effort. Weekly plans, user recipes, favorites, frequent recipes, and settings remain on their existing stores.
- Native iOS weekly-plan persistence now uses SwiftData. A single `WeeklyPlanRecord` stores the existing complete Codable `WeeklyMealPlan` snapshot, preserving AI-only recipes and all days, meals, shopping entries, servings, and ordering. Startup migrates `native_km_weekly_plan_v1` with `native_km_weekly_plan_swiftdata_migration_v1` while retaining legacy JSON; user recipes, favorites, frequent recipes, and settings remain on existing stores.
- The five native SwiftData modules have completed a consistency audit. All use one shared production/in-memory schema and independent MainActor contexts. Completed migration markers now self-heal from retained legacy JSON when their SwiftData table is unexpectedly empty; explicit clear-all removes both SwiftData records and legacy JSON so data cannot reappear. Five-module restart and backup restoration are covered by integration tests. Cross-context recovery remains best-effort rather than transactional.
- Native iOS recipe persistence has completed its sixth SwiftData phase. Full Codable user recipes are stored in `UserRecipeRecord`, while favorites and frequent-recipe flags share one independent `RecipePreferenceRecord` keyed by recipe ID, including remote-only IDs. `RecipeStore` keeps its public arrays/sets and all existing ID/source/content duplicate rules, but now writes the same application `ModelContainer` as the five `KitchenStore` modules. Startup migrates the three legacy keys behind `native_km_recipe_store_swiftdata_migration_v1`, retains them after successful migration, and can self-heal empty tables; explicit clear removes both tables and the legacy fallback. The version-1 kitchen backup still intentionally omits recipe-library data, favorites, and frequent flags because this phase preserves the established backup contract.
- Account login and PWA/iOS synchronization began from a completed architecture audit. The recommended future architecture remains Guest-first with Supabase Auth/Postgres, the existing Express server as an authenticated sync/API layer, per-account local stores, household-scoped kitchen data, user-scoped recipe preferences, UUID-based incremental synchronization, tombstones, optimistic concurrency, and explicit first-login merge previews. Only the server-side development authentication foundation is now verified; no client login, cloud kitchen business database, synchronization, or production authentication behavior has been enabled.
- The native-alignment Node baseline has been refreshed after the SwiftUI navigation and SwiftData transaction refactors. Settings, inventory entry/detail, Home status sheets, pantry staples, stock-in, and consumption checks now assert semantic wiring and reference the corresponding XCTest/XCUITest coverage instead of requiring obsolete constructor syntax, private method names, or UserDefaults-era mutation shapes. Production behavior was not changed.
- Account/sync implementation Phase 0 now provides Supabase local-project/migration structure, idempotent profile + personal-household initialization, constrained household membership, RLS policies, asymmetric JWKS JWT verification in Express, and an authenticated/rate-limited `GET /api/me`. Only the new probe endpoint requires authentication; PWA/iOS login, cloud kitchen tables, SwiftData changes, and synchronization remain unimplemented, and all existing Guest/public routes retain their behavior.
- Account/sync Phase 0.5 is now verified against the linked Supabase development project. Local/remote migration `20260713000100` match; a read-only remote query verifies all 3 identity tables, RLS, constraints/indexes, 3 triggers, the exact 9-policy set, and personal-household owner integrity. Two real users repeatedly pass Auth/JWKS, `/api/me`, bidirectional RLS isolation, owner/member boundary, anti-userID-forgery, and Guest-route smoke checks. Docker is unavailable, so local pgTAP was attempted but not executed; optional rate-limit saturation also remains unexecuted. This does not implement client login, cloud kitchen tables, or synchronization.
- Native iOS account Phase 1 now provides a Guest-first account entry, official `supabase-swift` email/password registration and sign-in, Keychain-backed session restoration/auth-state observation, explicit sign-out, and `/api/me` profile/household loading through the existing `APIClient`. Missing configuration and account-profile failures degrade safely without blocking local features. Authentication has no dependency on `KitchenStore` or the shared SwiftData container, so login and logout do not upload, clear, merge, or switch local kitchen data. Business synchronization, guest merging, invitations, OAuth, password reset, and account deletion remain unimplemented.
- Phase 1 development smoke verified Guest launch, real Supabase test-user sign-in, Keychain restoration after relaunch, sign-out back to Guest, and unchanged local inventory-backed recommendations. The current Render deployment returned `404` for `/api/me`; repository/local Phase 0 had already verified that route, so the remaining live client-to-profile gap is deployment parity rather than an iOS session failure.
- Account/sync Phase 2A-1 completed its repository-only schema audit and design draft for household inventory, shopping, today plans, consumption history, normalized weekly plans, user recipes, and user-scoped preferences. Direct authenticated DML remains deliberately closed; Phase 2A-2 adds the only proposed write surface through an atomic allowlisted RPC with `baseVersion` enforcement. No migration was pushed, no hosted Supabase object was changed, no local model was modified, and no automatic upload or synchronization was enabled.
- Account/sync Phase 2A-2 implemented the repository sync contract and Express foundation: authenticated bootstrap, cursor pull, and allowlisted batch mutation routes; strict payload limits; deterministic legacy UUID mapping; an atomic user-JWT RPC with idempotency, optimistic version checks, tombstones, and snapshot-backed change feed; and semantic Node/pgTAP coverage.
- Account/sync Phase 2A-2.5 has deployed migration `20260713000200` to the linked development Supabase project only. Remote object/grant/RLS verification, rollback-only pgTAP SQL, real two-user isolation, direct-DML denial, mutation create/update/conflict/delete/idempotency, scope-specific cursor pagination, all nine entity families, and local Express bootstrap/changes/mutations smoke pass. Household and personal data now use independent `(scopeType, scopeId)` cursors over one global BIGINT sequence. No production deployment, client SyncEngine, Guest upload/merge, or automatic sync is enabled.
- Account/sync Phase 2A-3 adds an iOS-only, disabled-by-default sync foundation: contract DTOs, arbitrary-precision cursor strings, separate SwiftData metadata/pending/cursor records, APIClient-backed transport, an explicit actor coordinator, and an inventory-only transaction POC. It is not wired to App startup, AuthStore, KitchenStore writes, timers, or background tasks; Guest data is not scanned or uploaded and all hosted writes remain disabled.
- Phase 2A-4 now has a Debug-only, explicitly gated inventory hosted-smoke runner for one default-household marker record. CoreSimulator was recovered on iPhone 17 Pro / iOS 27.0 and the real App -> development Render -> development Supabase lifecycle passed through bootstrap/create/pull/update/duplicate/conflict/soft-delete/final-pull plus metadata/pending/cursor and Guest-data boundary checks. Local and committed safety flags are back to `NO`. **Phase 2A-4 is checkpoint-complete**: final Node suite 786/786 passed (0 failed, 0 skipped); final serial iOS Unit/UI regression on the same simulator (`-parallel-testing-enabled NO`, full rebuild) reached 469 distinct tests with 0 failed and exactly 1 safe skip (`HostedSyncSmokeUITests`, not excluded from the run, skips cleanly without supplied credentials); `ReceiptCompactListUITests` passed in the full suite; final Debug build succeeded with 0 errors and 0 warnings; `npm audit` and `git diff --check` both clean. A local-only checkpoint commit was created and intentionally not pushed. See `docs/IOS_SYNC_PHASE2A4_VALIDATION.md` for the full audit trail. Not yet implemented: automatic/background sync, Guest data merge, additional repository adapters beyond inventory, conflict-resolution UI, and Realtime.
- Phase 2B-1 adds a user-initiated, explicitly-confirmed Guest inventory merge: read-only Guest data detection, a persisted `GuestMergeSession` state machine (11 states, bound to `userId`+`householdId`+`inventory_item`), pure local matching/plan generation with hash-based re-validation, explicit per-conflict user choice (`keepLocal`/`keepRemote`/`keepBoth`, partial commit supported), upload/rollback entirely through the existing `SyncCoordinator`/`InventorySyncAdapter`/`ExpressSyncTransport` (no second client), and a new independent `INVENTORY_SYNC_ENABLED` flag (default `NO` everywhere) that gates the feature separately from `SYNC_ENABLED`. Only inventory is ever touched; Shopping/Today Plan/Weekly Plan/Recipes are counted for display only. This round is mock/UI-tested and disabled by default only — no real hosted Guest merge, test account, or remote write was performed; that is Phase 2B-2. See `docs/GUEST_MERGE_PHASE2B.md` and `docs/INVENTORY_MERGE_CONTRACT.md`.
- **A Phase 2B-1 corrective/hardening pass** followed a design review: (1) confirmed `quantity` was never actually part of the matching key (only `normalizedName + unit`), but the classifier was rewritten to explicitly distinguish expiry-compatibility (both absent/equal vs. one-absent/different) from quantity conflicts from a new `metadataMismatch` conflict reason (`isStaple`/staple category/threshold/restock/tracking/availability differences), so a same-key-different-id match is never silently narrowed to a specific field-level reason and a metadata-only difference is never silently overwritten by an upload; (2) replaced the `AccessTokenReader` closure (which a `View` called `authStore.currentAccessToken()` directly to build) with `confirmMerge(authStore:)`/`rollback(authStore:)` taking the live `AuthStore` reference and a private `AuthStoreCredentialProvider` that holds only a `weak var authStore` and re-queries the token fresh per network call, so a `View` never sees a token value and a sign-out mid-run starves further requests instead of reusing a stale token. 13 new XCTest cases were added covering the matching/expiry/metadata rules, sign-out refusing `confirmMerge`/`rollback`, the snapshot size cap never truncating the actual merge plan, corrupted session-record data decoding safely without offering a plan to upload, and plan-hash order-independence/invalidation on expiry-change/removal/addition; 3 new Node semantic-guard assertions confirm no View file calls `currentAccessToken()` directly. Full regression after the fixes: Node 802/802 passed, iOS Unit 502/502 + UI 4 (1 safe skip, 0 failures) passed, Debug build 0 errors/0 new warnings, `npm audit` and `git diff --check` clean. Still mock/UI-tested and disabled by default only — no real hosted Guest merge, test account, or automatic sync was enabled; that was Phase 2B-2.
- **Phase 2B-2 ran a controlled, real hosted Guest inventory merge smoke** against the development Supabase project through the existing Render deployment, using a new Debug-only `GuestMergeSmokeRunner`/`HostedGuestMergeSmokeTests` harness (mirrors the Phase 2A-4 `SyncSmokeRunner`/`HostedSyncSmokeUITests` pattern; gated by a new independent `GUEST_MERGE_SMOKE_ENABLED` flag, always `NO` in committed configuration). Completing this required finishing a piece Phase 2B-1 deliberately deferred: `GuestMergeController.preparePreview` gained an optional `remoteTransport` parameter (default `nil`, zero behavior change for ordinary in-app preview) that performs one read-only pre-merge `fetchChanges` pull to populate `knownRemoteItems`, and `RemoteInventorySnapshotItem`/`InventoryMergeCandidate` gained a `remoteVersion` field so `confirmMerge` can seed the correct baseVersion for a same-id update whose remote existence this device only just learned about (previously would have sent a stale `baseVersion 0` and been correctly rejected). Using an isolated, in-memory `KitchenStore`/persistence container the harness itself builds (never the developer's real local Guest inventory) and two real development test accounts, the run verified: zero-write preview, create, duplicate-retry idempotency, quantity/expiry/metadata conflict detection against real pre-existing remote counterparts, ambiguous-duplicate never auto-selected, plan-drift invalidation, restart recovery, logout refusing further requests then resuming after re-login, real cross-account isolation (a second account's own bootstrap returns a different household and cannot see the first account's session), rollback (soft-delete via tombstone, confirmed by a final real pull), and the Guest data boundary unchanged throughout. A confirmed follow-up (fixed in Phase 2B-2.5, below): `keepBoth` on a same-id conflict did not allocate a new id, so it was not actually well-defined outside the different-id ambiguous case. All marker rows were soft-deleted (confirmed via a one-off cleanup script using only the authorized user-level API); `INVENTORY_SYNC_ENABLED`/`GUEST_MERGE_SMOKE_ENABLED` were restored to `NO` in the ignored `Local.xcconfig`. Final regression: Node 802/802, iOS Unit 507/507 (1 safe skip) + UI 4 (1 safe skip), Debug build 0 errors/0 new warnings, `npm run smoke:sync` passed, `npm audit` clean, `git diff --check` clean. See `docs/GUEST_MERGE_PHASE2B2_VALIDATION.md`. No test account was created, no automatic sync was enabled, and nothing was pushed.
- **Phase 2B-2.5 fixed the same-id `keepBoth` identity-fork gap** the Phase 2B-2 hosted run confirmed. `InventoryMergeCandidate` gained `forkedLocalItemId: UUID?` (part of the already-persisted plan, no new SwiftData model): `applyingChoice(.keepBoth)` on a same-id match now allocates a fresh, stable id once (`forkedLocalItemId ?? UUID()`) and reuses it verbatim on every later call (repeated choice, repeated confirm, App restart); the different-id ambiguous-duplicate case is completely unaffected. `GuestMergeController.confirmMerge`'s staging loop stages the forked id as a plain create (baseVersion 0, guarded against re-staging on retry) and never touches the original, certain remote entity for that candidate (a true no-op, like `keepRemote`); the read-back loop keys `createdEntityIds` off the forked id, so `rollback` only ever removes the fork, never the original. The original local Guest `InventoryRecord`'s id is never mutated; the fork is a genuinely independent second local record. Added 8 new offline `GuestMergeTests` cases and 6 new Node semantic-guard assertions (all passing, alongside the full existing suite). A minimal, dedicated real hosted smoke (`HostedGuestMergeSmokeTests.testControlledDevelopmentSameIdKeepBothIdentityFork` — never a repeat of the full Phase 2B-2 matrix) confirmed on the real development backend that a same-id `keepBoth` conflict produces two real, distinct remote records, and that rollback removes only the fork; passed on the first attempt, zero marker rows left afterward. Safety flags restored to `NO`. Final regression: Node 808/808, iOS Unit 515/515 (2 safe skips) + UI 4 (1 safe skip), Debug build 0 errors/0 new warnings, `npm run smoke:sync` passed, `npm audit` clean, `git diff --check` clean. See `docs/GUEST_MERGE_PHASE2B2_VALIDATION.md`. Still not entering Phase 2B-3; nothing pushed.
- **Phase 2B-3 turned the validated Phase 2B-1/2B-2 merge engine into a formal, user-facing UI**: a non-blocking "发现本地库存" prompt card, an enriched preview (target household name, cloud-side known-item count, per-type conflict breakdown), a conflict screen showing local-vs-remote quantity/expiry side by side with a new fourth choice `.skip` ("稍后处理"), a `keepBoth`-same-id fork notice, and a manual "立即同步库存" entry (`GuestMergeController.syncNow`, the only production `runOnce` call site besides `confirmMerge`/`rollback`, scoped to `.inventoryItem` only). A new independent flag `INVENTORY_MERGE_UI_ENABLED` (default `NO` everywhere) gates the UI separately from `INVENTORY_SYNC_ENABLED` (the network capability) — both must be on for anything to show or write. Decided but deliberately deferred: whether ordinary post-merge Inventory CRUD should auto-generate `PendingMutation`s — the conservative policy is documented, but wiring it into `KitchenStore` would require threading Auth/sync context into an intentionally decoupled component, a bigger architecture change left for a future phase. Added 7 new `GuestMergeTests` cases, 19 new Node semantic-guard assertions, and 1 real credential-free XCUITest verifying the Guest-mode "我的" tab shows no merge/sync UI before sign-in. Full regression: Node 822/822, iOS Unit 522/522 (2 safe skips) + UI 5/5 (1 safe skip), Debug build 0 errors/0 new warnings, `npm run smoke:sync` passed, `npm audit` clean, `git diff --check` clean. See `docs/INVENTORY_SYNC_PHASE2B3.md`. Still no automatic sync anywhere; nothing pushed.
- **Phase 2B-4 wired ordinary Inventory CRUD to the sync engine**, closing the boundary Phase 2B-3 deliberately deferred. New `InventorySyncEnrollment` (persisted per `userId`+`householdId`; only `confirmMerge`'s success branch ever moves it to `.enrolled`) and a centralized `InventorySyncEligibility.evaluate(...)` decide, once and in one place, whether a create/update/delete is allowed to stage a `PendingMutation` — never duplicated inline in a View or CRUD method. `KitchenStore` gained exactly one optional, generic hook (`onInventoryChanged: (([InventoryItem], [InventoryItem]) -> Void)?`, vocabulary limited to `[InventoryItem]`, no Auth/Sync import) wired once in `ContentView.swift`'s composition root. New `SwiftDataSyncPersistence.stageInventoryMutation(...)` atomically writes `SyncMetadata`+`PendingMutation` with full coalescing (create+update, create+delete cancellation, update+update, update+delete-as-tombstone, duplicate-delete no-op) — deliberately never touching `InventoryRecord` itself, since `KitchenStore`'s own persistence already wrote it through a separate `ModelContext` (avoiding a dual-context race identified during the pre-implementation audit). Delete always stages a soft-delete tombstone, never a physical remote delete. Added 21 new `GuestMergeTests` cases, 14 new Node semantic-guard assertions, and a minimal real hosted smoke (`__inventory_crud_smoke_<marker>`: create/update/delete each verified through manual sync, remote version advancing correctly, a repeat sync as a harmless no-op, and a Guest-only control item confirmed never staged) — passed on the first attempt, zero marker residue (the cleanup script was extended to sweep this new marker prefix too). Full regression: Node 836/836, iOS Unit 540/540 (3 safe skips) + UI 5/5 (1 safe skip), Debug build 0 errors/0 new warnings, `npm run smoke:sync` passed, `npm audit` clean, `git diff --check` clean. See `docs/INVENTORY_CRUD_SYNC_PHASE2B4.md` and `docs/INVENTORY_MUTATION_COALESCING.md`. Still no automatic sync anywhere; nothing pushed.
- **Phase 2B-5 is a release-readiness/dogfood-safeguard audit, not a feature phase — conclusion is No-Go for production.** Added a dedicated, safe-off-by-default `InventorySyncDogfoodConfiguration` (`INVENTORY_SYNC_DOGFOOD_ENABLED`, `INVENTORY_SYNC_DIAGNOSTICS_ENABLED`, both `NO`) that only ever unlocks a read-only diagnostics screen and confirms existing manual paths, never automatic sync. New `InventorySyncEligibility.blockedByQueueFull` bounds the pending-mutation queue (never drops a delete, never blocks coalescing into an already-staged row). New `InventorySyncDiagnosticsSnapshot` (redacted — no email/token/full UUID/payload/item name) and `InventorySyncConsistencyChecker` (14 read-only checks, never auto-fixes) back a new dogfood-gated "库存同步诊断" screen at the bottom of the account page, offering only refresh/export/retry-sync/help. Verified single-flight on `syncNow` by test rather than adding a new gate actor. Added 10 new `GuestMergeTests` cases (82/82 passing, up from 72); full iOS Unit regression 550/550 (up from 540), 0 regressions. Explicitly **not** done this phase: weak-network/error-injection tests, performance/scale tests at 500-1000 items, a production config audit, physical-device dogfood validation, and the hosted development-environment dogfood smoke — see `docs/INVENTORY_SYNC_GO_NO_GO.md` for the full gap list. Status to report anywhere: "simulator dogfood passed, physical-device validation pending" — never "production ready." See `docs/INVENTORY_SYNC_RELEASE_READINESS_PHASE2B5.md`, `docs/INVENTORY_SYNC_DOGFOOD_PLAYBOOK.md`, `docs/INVENTORY_SYNC_ROLLBACK_PLAYBOOK.md`, `docs/INVENTORY_SYNC_DIAGNOSTICS.md`, `docs/INVENTORY_SYNC_GO_NO_GO.md`. All flags remain `NO`; nothing pushed.
- **Phase 2B-6 closed every Phase 2B-5 evidence gap this environment can close — conclusion is now Dogfood Go / Production No-Go, pending only physical-device validation.** Added a test-only `InventorySyncFaultInjectingTransport` (lives solely in `KitchenManagerTests`, cannot compile into the app target) covering offline/401/403/413/429/500/503/malformed-truncated-JSON/push-applied-then-client-timeout/pull-succeeded-then-local-save-failure/app-killed-before-cleanup — every case confirmed pending-retaining, cursor-safe, and duplicate-safe on retry. Verified single-flight under real concurrency (`withTaskGroup`, 10 concurrent taps → exactly 1 `sendMutations` call) and that a scope-mismatch never sticks the guard. Added a queue-cap-at-scale test (250 attempted creates against a 200 cap holds firm; deletes/coalescing still succeed). Added scale/performance sanity tests at 1000 metadata rows / 500 pending mutations / 100 conflicts — no O(n²) hotspot found. Ran a **real hosted development dogfood smoke** (`GuestMergeSmokeRunner.runInventoryDogfoodMinimalSmoke`) against the actual development Supabase project and Render deployment using an isolated `__inventory_dogfood_<id>` marker — full create/sync/update/sync/offline-stage/reconnect+sync/simulated-restart/duplicate-safe-retry/delete/sync/tombstone/diagnostics-clean/consistency-clean/cleanup flow passed on the first attempt with zero marker residue; flags were only ever set to `YES` in the gitignored `Local.xcconfig` for the run's duration and restored to `NO` immediately after. Performed a read-only production-config audit (Supabase/Render URL injection, JWKS/issuer, RLS migration status, service-role absence, logging redaction — no Blocker found) and built + inspected a real unsigned Archive (all 8 sync/dogfood/smoke flags confirmed `NO`; zero test credentials/emails/smoke-markers found in the compiled binary via `strings`). Added 17 new `GuestMergeTests` cases (99/99, up from 82) and 9 new Node semantic-guard assertions (854/854). Full iOS Unit regression 568/568 (up from 550), 0 regressions. Physical-device validation was **not** attempted — no device is attached to this environment; a ready-to-run 30-step checklist was prepared instead (`docs/INVENTORY_SYNC_PHYSICAL_DEVICE_CHECKLIST.md`). Status to report anywhere: "simulator + hosted-development dogfood passed, physical-device validation pending" — never "production ready." See `docs/INVENTORY_SYNC_PHASE2B6_VALIDATION.md`, `docs/INVENTORY_SYNC_FAULT_INJECTION.md`, `docs/INVENTORY_SYNC_SCALE_RESULTS.md`, `docs/INVENTORY_SYNC_PRODUCTION_CONFIG_AUDIT.md`, `docs/INVENTORY_SYNC_PHYSICAL_DEVICE_CHECKLIST.md`, and the updated `docs/INVENTORY_SYNC_GO_NO_GO.md`. All flags remain `NO`; nothing pushed.
- **Phase 2B-7 ran real physical-device validation — conclusion remains Dogfood Go / Production No-Go, but the gap narrowed to only the human-gesture UI layer.** A physical iPhone 17 Pro (iOS 27.0) became available; the full `GuestMergeTests`/`HostedGuestMergeSmokeTests` XCTest suites ran for real *on that hardware* (real SwiftData, real Keychain, real HTTP calls over the device's own network) — 97/99 passed, with the 2 failures being expected side effects of intentionally setting dogfood flags to `YES` for the run (tests that assert the disabled-by-default state), not a product bug. The hosted development dogfood smoke ran for real from the device itself against the real Render deployment/development Supabase project — passed, zero marker residue. A genuine OS-level app-kill/relaunch cycle was performed via `devicectl terminate`/`launch` on the real installed process (new PID, no crash). A safety finding was caught and corrected mid-phase: the connected device was the operator's own daily-use phone already signed into a real account, so all data-touching validation ran only inside the isolated XCTest sandbox (never the real installed app's data), and — since building with the dogfood flags set to `YES` meant the *compiled binary actually on the phone* carried those flags — a second Debug build with flags restored to `NO` was verified via `plutil` and reinstalled over the dogfood build before ending the phase, preserving the operator's own app data. What remains unautomatable in this environment (no touch/tap-injection tool for a physical device): tapping through the Guest-merge UI, a real Airplane Mode toggle, a real screen lock/unlock, and Instruments-based memory profiling — all honestly reported as BLOCKED (tooling), not faked as passing. Full regression re-confirmed unchanged: Node 854/854, iOS Unit 568/568 (4 safe skips), UI 5/5 (1 safe skip), `GuestMergeTests` 99/99 (on simulator, flags back to `NO`), 0 regressions. See `docs/INVENTORY_SYNC_PHYSICAL_DEVICE_RESULTS.md` and `docs/INVENTORY_SYNC_FINAL_GO_NO_GO.md`. All flags remain `NO`; nothing pushed.
- **Phase 2B-7 continued with a full manual, human-driven physical-device round — one real crash bug found and fixed; conclusion still Dogfood Go / Production No-Go, gap narrowed further to only Conflict UI + Rollback.** The device's operator personally tapped through, one step at a time: sign-in, Guest-merge prompt/skip/re-entry-recovery/preview, explicit confirm (after being asked and consenting, since this device's local inventory isn't partitioned per account and confirming uploads whatever's locally stored to the TEST_USER_A household on the *development* project), marker create/update/delete + manual sync, Airplane-Mode offline edit + reconnect + sync, Wi-Fi/cellular switch, backgrounding mid-sync, lock/unlock, force-quit-with-pending + relaunch + sync, User A/B account switch with isolation confirmed (User B correctly showed unmerged state, no leak of User A's synced status; User A's state correctly recovered on re-login), the real diagnostics screen and its export JSON (manually reviewed — only counts/statuses/timestamps, no email/password/token/full UUID/household ID/mutation ID/item name), and final marker cleanup. **A real, previously-unknown crash was found**: deleting an inventory item from its own detail screen (after that screen rendered a Toggle) crashed the app twice — root-caused from real crash logs pulled directly off the device (`devicectl device info files --domain-type systemCrashLogs`) to a stale array-index captured by a `Binding` closure in `InventoryItemDetailView` (`KitchenManager/PantryStaples.swift`), a pre-existing bug unrelated to sync/dogfood, reachable in ordinary use. Fixed by resolving every field binding fresh by item id instead of a captured index; added a regression UI test (`testDeletingInventoryItemAfterTogglingStapleDoesNotCrash`) reproducing the exact sequence; re-verified on simulator then redeployed and re-verified for real on the same physical device — only the one failed step was retried, per "不重跑无关步骤." Two items were explicitly **not** exercised rather than guessed at: Conflict UI (no naturally-occurring conflict existed to trigger it) and Rollback (a stale/cached preview screen was encountered while looking for the entry point; the operator correctly backed out without acting rather than risk ambiguous state). `scripts/cleanup-guest-merge-smoke-markers.mjs` gained the `__inventory_device_dogfood_` marker prefix and confirmed 0 residual rows. Device rebuilt and reinstalled with all flags back to `NO`, verified via `plutil`; final app launch showed a clean Guest sign-in prompt with the test account cleanly signed out and no crash. Full regression re-confirmed: Node 854/854, iOS Unit 568/568 (4 safe skips), UI 6/6 (1 safe skip, up from 5 — the new regression test), Debug/Release clean builds 0 warnings/0 errors, 0 regressions. See `docs/INVENTORY_SYNC_PHYSICAL_DEVICE_RESULTS.md` and `docs/INVENTORY_SYNC_FINAL_GO_NO_GO.md`. All flags remain `NO`; nothing pushed.
- **Phase 2B-7's final round attempted Conflict UI and Rollback specifically — Conflict UI is a confirmed production release blocker, Rollback remains untested; conclusion unchanged at Dogfood Go / Production No-Go.** Seeded one marker item into `TEST_USER_B`'s development-project household via the authenticated user-level API (no service-role) and had the operator create a matching local item (same name/unit, different quantity) to engineer an ambiguous-duplicate conflict scenario. The real "查看并合并" preview still showed zero conflicts. Tracing the full call chain (`GuestMergePromptView` → `GuestMergeController.preparePreview` → `fetchKnownRemoteItems` → `SyncTransport.fetchChanges`) confirmed the production UI's single call site never passes a `remoteTransport`, so preview is genuinely **zero-network** (not merely zero-write) and `knownRemoteItems` is always empty — meaning the thoroughly-unit-tested conflict-detection logic in `InventoryMergePlanner` is **structurally unreachable from the shipped app**, and because the server only enforces per-`entityId` optimistic concurrency (no business-key deduplication), a same-named item with a different id can be silently created as a duplicate. **This is now classified as a release blocker**, not merely an untested item — see `docs/INVENTORY_MERGE_REMOTE_PREVIEW_FIX_DESIGN.md` for the fix design (not yet implemented). **Correction to an earlier real-time assessment**: a subsequent read-only reconciliation against the full remote change feed found that the two personal-local inventory items the operator noticed during this round's confirm **already existed in that household from well before this round's confirm** — the earlier claim that "this round's confirm inadvertently uploaded 2 real personal items" is **not supported by the evidence** and is corrected here; their true origin remains unknown, and the change feed shows no create/update for them during this round's confirm window. The "预计新增3条 → 已合并1条" discrepancy likewise remains **unresolved** — available read-only remote evidence cannot identify which write (if any) the reported "1条" corresponds to, and this was not chased further given no local-device session/metadata access was available. Per the "stop when state is unclear" rule, Rollback was **not** attempted — not because rollback logic failed, but because this session's `createdEntityIds` cannot be proven from available read-only evidence, so **rollback scope cannot be safely confirmed (UNSAFE TO ROLLBACK)**. Cleanup: the one item this round's script created was soft-deleted by exact name match (verified 1/1); all other items were deliberately left untouched given unproven provenance. Device rebuilt with all flags back to `NO`, verified via `plutil`, reinstalled; final launch confirmed Guest prompt, no crash. Full regression re-confirmed: Node 854/854, `npm run smoke:sync` PASS, iOS Unit 568/568 (4 safe skips), UI 6/6 (1 safe skip), Debug/Release clean builds 0 warnings, 0 regressions. See `docs/INVENTORY_SYNC_PHYSICAL_DEVICE_RESULTS.md`'s "Conflict UI and Rollback attempt" section (now annotated with this correction), the updated `docs/INVENTORY_SYNC_FINAL_GO_NO_GO.md`, and `docs/INVENTORY_MERGE_REMOTE_PREVIEW_FIX_DESIGN.md`. Status to report anywhere: "Conflict UI is a confirmed production release blocker (structurally unreachable preview + no server-side dedup = silent-duplicate risk); Rollback still untested and unsafe to attempt on the current session" — never "production ready." All flags remain `NO`; nothing pushed.
- **Phase 2B-8 fixed the Phase 2B-7B release blocker in code: the production preview now performs a real, authenticated, read-only remote read, so the Conflict UI is reachable and the silent-duplicate risk is closed at the code level — still Dogfood Go / Production No-Go pending physical-device re-verification and Rollback.** `GuestMergePromptView`'s `.task` now calls a new production-only overload, `GuestMergeController.preparePreview(userId:householdId:kitchenStore:authStore:)`, which builds a real `AuthStoreCredentialProvider`/transport internally (the View still never touches a token) instead of the old always-`nil`-transport call, so `knownRemoteItems` reflects real household state and the already-unit-tested quantity/expiry/metadata/ambiguous-duplicate detection in `InventoryMergePlanner` is now reachable from the shipped app. Two silent-failure bugs in `fetchKnownRemoteItems` were fixed to `throw` instead of silently degrading: a scope mismatch, and exceeding the hardcoded `maxPages` cap while more remote data remained (both previously risked returning a partial/wrong snapshot indistinguishable from "empty household"). A remote-fetch failure now sets a new, dedicated `previewFetchFailureMessage` (mapped through the existing `userFacingSyncError`), never touches an existing session, and a new `InventoryMergePreviewFetchFailureView` renders it with a retry action — taking precedence over both the empty-state and any stale prior plan. `InventoryMergePlan`/`InventoryMergePlanner` gained a canonical, order-independent `remoteSnapshotHash` (SHA256 over sorted entity id/name/unit/quantity/expiry/staple-fields/`remoteVersion`) folded into `planHash`, so remote drift (a version bump, create, or delete) invalidates a plan exactly like local drift already did (`isPlanStillValid` now takes an optional `currentRemoteItems`). `confirmMerge` now re-fetches and re-hashes the remote snapshot immediately before staging any mutation, rejecting with "家庭库存已变化，请重新预览" and reverting to `previewReady` on any mismatch — before any `stageUpsert`/`stageDelete`/`sendMutations` call — plus a session-owner (`userId`) identity guard. Discovered and fixed a real regression risk during this work: `preparePreview`'s existing local-drift check wasn't passing the freshly-fetched remote items into `isPlanStillValid`, so any plan with a non-nil remote fingerprint was being spuriously regenerated on every call (silently discarding resolved conflict choices and same-id `keepBoth` forked ids) — fixed by threading `knownRemoteItems` through. Added 19 new `GuestMergeTests` cases (118/118, up from 99) covering production-overload wiring, token-boundary, scope-mismatch/pagination-cap/401/offline/malformed-response preview failures, zero-write/zero-cursor-advance during preview, fingerprint determinism/order-independence/version-change/create-delete-change, remote-drift plan invalidation, stale-confirm rejection (zero `PendingMutation`, zero `sendMutations` calls), household isolation, restart fingerprint recovery, and the specifically-required `testProductionPreviewDoesNotSilentlyCreateBusinessEquivalentRemoteItem` regression test — plus 10 new Node semantic-guard assertions (864/864, up from 854). Full regression: Node 864/864, iOS Unit 583/583 (up from 568, 0 regressions), Debug/Release clean builds 0 errors. **Explicitly not done this phase** (deferred, not blocking, given the effort budget): the 16 named UI test scenarios from the design spec (the UI test target was verified to still build and pass unchanged, but no new dedicated UI cases were added for the new fetch-failure view/conflict-reachability paths), and the hosted minimal validation (section 十八 — requires live infrastructure and explicit user-driven marker seeding/cleanup). Physical-device re-verification of the Conflict UI and Rollback (both still open from Phase 2B-7) also remain outstanding. See `docs/INVENTORY_MERGE_REMOTE_PREVIEW_PHASE2B8_VALIDATION.md`, the updated `docs/INVENTORY_MERGE_REMOTE_PREVIEW_FIX_DESIGN.md`, `docs/INVENTORY_MERGE_CONTRACT.md`, and `docs/INVENTORY_SYNC_FINAL_GO_NO_GO.md`. Status to report anywhere: "the Conflict-UI release blocker is fixed in code and simulator-validated; hosted-development and physical-device validation are still pending — never 'production ready.'" All flags remain `NO`; nothing pushed.

---

## 2. Verified Current Project Shape

Current observed structure:

- Root frontend files: `index.html`, `app.js`, `styles.css`.
- PWA files: `manifest.webmanifest`, `sw.v18.js`, `sw-register.v18.js`, `sw-reset.html`.
- Backend entry: `server.js`.
- Frontend/domain modules: `src/*.js`.
- Page views: `src/views/*`.
- UI components: `src/components/*`.
- Server modules: `src/server/*`, `src/server/services/*`, `src/server/utils/*`.
- Data files: `data/*`.
- Scripts: `scripts/*`.
- Tests: `test/*`.
- Docs already present: `README.md`, `PROJECT_GUIDE.md`, `PROJECT_GUIDE.zh.md`, `PROJECT_WORKFLOW.md`, `docs/*`.
- CI/deployment: `.github/workflows/deploy.yml`.
- Current stable routes: empty hash redirects to `#today`; `#today` is the kitchen home, `#inventory` is food inventory, `#shopping` is shopping, `#recipes` is the recipe library, and `#settings` is settings.
- Weekly menu distinguishes meal batches from dishes: `mealCount` is the number of meals, `dishesPerMeal` is the target dishes per meal, and `mealIndex` only groups transient suggestions. `plan` remains one independent recipe row per dish.

Current package facts:

- Package name: `kitchenmanager`.
- Runtime: Node >= 18.
- Main local command: `npm start` -> `node server.js`.
- Main test command: `npm test` -> `node --test`.
- Validation commands:
  - `npm run validate:recipe-packs`
  - `npm run validate:recipe-pack-data`
- Current dependencies include Express, Axios, and ffmpeg-static.

---

## 3. Existing Strengths

- The app already has a clear product direction: local-first kitchen inventory, recipes, shopping list, planning, AI assistance, and backup.
- The project already has detailed architecture guidance in `PROJECT_GUIDE.zh.md` and `PROJECT_WORKFLOW.md`.
- The repository contains a broad Node test suite under `test/*`.
- GitHub Pages deployment has a test gate.
- The README already documents local run modes, AI settings, localStorage keys, PWA/cache behavior, and deployment options.
- The codebase has a modular structure with domain modules, views, components, server services, and tests.

---

## 4. Current Risk Areas

These areas need extra caution:

### Data safety

- `localStorage` keys are user-data contracts.
- Any schema change needs migration and backup/restore review.
- Never clear user data to fix a bug.
- API Key must not be written to backup, logs, or committed defaults.

### Shopping item shape

- Shopping item normalization/rebuild behavior can silently drop new fields if the fixed field set is not updated.
- Any shopping item field change needs refresh/persistence testing.

### AI import and media pipeline

- Link/video extraction may be incomplete or unreliable.
- AI-generated recipe data must remain reviewable draft data.
- Media and URL handling must preserve SSRF/rate-limit/error handling safeguards.
- Temporary `creative-*` AI recommendation ids are display-only. They must be saved as a unique overlay recipe before entering a plan or becoming editable.

### PWA cache/versioning

- Browser/Service Worker caches can keep old files alive.
- Frontend file changes may require version stamping and possibly Service Worker cache updates.

### Architecture drift

- The project is intentionally no-framework and no-build.
- Avoid gradually introducing framework-like patterns, duplicated domain logic, or Tailwind-style classes that do not work in this project.
- Keep route documentation, comments, and tests aligned with the stable five-entry navigation; do not swap the meanings of `#today`, `#inventory`, and `#shopping` without product confirmation.

---

## 5. In Progress

- Adding unified AI-agent handoff documentation:
  - `AGENTS.md`
  - `AI_CONTEXT.md`
  - `PROJECT_STATUS.md`
  - `CODING_RULES.md`
  - `TESTING_RULES.md`
  - `CHANGELOG.md`
  - optional tool adapters such as `CLAUDE.md` and `.cursorrules`
- The native iOS home recommendation module now has ranked local search, cancellable AI supplementation/new-batch generation, native paged cards, and in-place today-plan actions. Persistence for recommendation favorites/dislikes remains future work.
- The native five-tab shell uses the system iOS 27 tab-bar scroll minimization behavior; no custom tab bar or manual scroll-direction state is present.
- The native link-import feature now forms a complete flow from Render extraction and AI parsing through editable preview to persisted user recipes. Remote recipe refreshes and local user recipes remain separate and are merged for display.
- The native “AI 做菜” task now generates a complete editable recipe from inventory and user constraints through the existing Render AI proxy, then supports saving, adding to today, or both. Regeneration preserves the input and excludes the current dish name.
- The native food-entry task now supports camera or photo-library receipt capture, local image normalization/compression, real vision recognition through the existing Render `/api/ai-chat` receipt task, editable item confirmation, and persistent batch inventory import. The simulator correctly disables camera capture while retaining photo-library and manual entry.
- The native Xiaohongshu/web recipe importer now uses the backend's complete `/api/recipe-import-from-url` pipeline, accepts full share text, persists canonical/original source metadata, blocks duplicate source imports, exposes understandable progress/errors, and keeps the existing editable-review-before-save flow.

---

## 6. Recommended Next Priorities

1. Add the shared AI-agent documentation files to the repository root.
2. Run `npm test` after adding the documentation, even though docs should not affect runtime behavior.
3. Open the app locally with `npm start` and confirm the main page loads.
4. Keep `PROJECT_GUIDE.zh.md`, `PROJECT_WORKFLOW.md`, and the new agent docs aligned when major rules change.
5. For future coding tasks, require the AI tool to read `AGENTS.md` first.
6. When changing frontend JS/CSS, remember cache/version stamping rules.
7. When changing data structures, add tests and update backup/migration behavior.

---

## 7. Do Not Change Without Explicit Approval

- `S.keys` storage key strings.
- Hash routes and bottom navigation semantics.
- User backup/export format.
- API Key storage/export behavior.
- GitHub Pages deployment workflow.
- Package manager / lockfile strategy.
- Framework architecture.
- PWA Service Worker strategy.
- Recipe source data files as a substitute for user overlay edits.

---

## 8. How to Update This File

After each meaningful change, update:

- `Last updated` date.
- `Current Stage` if the project phase changed.
- `In Progress` if active work changed.
- `Recommended Next Priorities` if priorities changed.
- `Current Risk Areas` if a new risk is discovered.

Do not turn this file into a long changelog. Put detailed change history in `CHANGELOG.md`.
# Native iOS prototype (2026-07-10)

- Added a standalone SwiftUI prototype under `ios-native/Kitchen Manager`.
- Split the native app into app shell, recipe models/store, API services, home, recipe, and add/import views.
- The Xcode `KitchenManager` scheme builds successfully with no issues in Xcode.
- Native persistence, inventory, shopping, meal planning, and full parity with the PWA remain future work.
- The native tab structure now matches the PWA: Today, Inventory, Shopping, Recipes, and Settings; recipe creation lives inside Recipes instead of occupying a tab.
- Inventory, shopping, and settings have native information-architecture shells ready for SwiftData-backed behavior.
- The native Today page now follows the PWA's current structure: greeting/status header, expiry and pending-shopping pills, Plan/Recommendations segmented panel, recipe search and cycling, weekly-menu entry, and the two quick actions.
- Added native modal flows for expiry details, pending shopping, manual/receipt inventory entry, recipe preview, all recommendations, recommendation actions, cooked-meal calibration, weekly planning, quick shopping entry, and recipe import.
- Added a shared `KitchenStore` with local persistence so inventory, today's plans, and shopping state are reflected across Today, Inventory, Shopping, and Settings.
- Re-audited the native Today page against the deployed PWA at a 390px mobile viewport and the final effective CSS rules, rather than the older home prototypes still present earlier in `styles.css`.
- Added a shared native `AppTheme` sourced from the PWA tokens (`#007AFF` primary, `#34C759` success, `#FF9500` warning, adaptive surfaces/text) and rebuilt the Today background, header, status pills, custom segmented control, search panel, white recommendation card, pagination, quick actions, and floating dock to match the deployed mobile layout.
- Kept the app on a native SwiftUI `TabView`, using page-style selection plus a stable SwiftUI floating dock so there is no WebView and no duplicate system tab bar.
- Verified the result by building and running on the iPhone 17 Pro simulator and comparing a simulator screenshot with the deployed PWA screenshot.
