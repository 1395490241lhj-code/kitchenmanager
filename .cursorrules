Use `AGENTS.md` as the single repository instruction entry point for Cursor.

Before editing:
- Read `AGENTS.md` completely.
- Follow its minimum-reading and task-routing rules.
- Inspect affected code, tests and current worktree state.

Do not duplicate project facts or phase status here. Follow the authorization boundaries and final-report format in `AGENTS.md`.
